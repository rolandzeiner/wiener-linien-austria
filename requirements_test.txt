# Minimum-version floors so a transitive minor release of any of these
# can't break green-on-main without a commit on our side. Bump when the
# project intentionally adopts new behaviour from a newer release.
# Local dev tracks the floor or higher; CI installs the floor's minor
# resolved by pip.
pytest-homeassistant-custom-component>=0.13.329,<0.14
pytest-asyncio>=1.3.0,<2
pytest-cov>=7.1.0,<8
syrupy>=5.1.0,<6
mypy>=2.0.0,<3
ruff>=0.15.12,<1
# Floors track PHACC 0.13.329's hard pins (pytest-cov==7.1.0,
# syrupy==5.1.0, pytest-asyncio==1.3.0). PHACC ≥0.13.317 wraps HA core
# 2026.5+ and requires Python ≥3.14, so the CI workflow bumped to 3.14
# in lockstep — see validate.yml's tests job.
