# Minimum-version floors so a transitive minor release of any of these
# can't break green-on-main without a commit on our side. Bump when the
# project intentionally adopts new behaviour from a newer release.
# Local dev tracks the floor or higher; CI installs the floor's minor
# resolved by pip.
pytest-homeassistant-custom-component>=0.13.311,<0.14
pytest-asyncio>=1.3.0,<2
pytest-cov>=7.0.0,<8
syrupy>=5.0.0,<6
mypy>=1.20.2,<2
ruff>=0.15.12,<1
# pycares cap removed 2026-05-04: PHACC 0.13.311 pins HA core 2026.2.0b2
# which already pins aiodns==4.0.0; aiodns 4 uses the pycares 5 API
# natively (no more ares_query_a_result reference). The transitive
# resolver now picks pycares 5.x without breaking pytest collection.
